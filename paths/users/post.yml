post:
  summary: Create a  new user
  tags:
    - Users
  requestBody:
    required: true
    content:
      application/json:
        schema:
          $ref: "../../components/schemas/user.yml#/CreateUserReq"
  responses:
    "201":
      description: Created
      content:
        application/json:
          schema:
            $ref: "../../components/schemas/user.yml#/CreateUserRes"
