GetQuestionCardRes:
  type: object
  properties:
    questionCards:
      type: array
      items:
        type: object
        properties:
          questionCardId:
            type: string
            format: uuid
          questionCardText:
            type: string
          isUsed:
            type: boolean
        required:
          - questionCardId
          - questionCardText
          - isUsed
