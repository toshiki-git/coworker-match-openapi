post:
  summary: Create a new matching
  tags:
    - Questions
  requestBody:
    required: true
    content:
      application/json:
        schema:
          $ref: "../../components/schemas/matching_question.yml#/CreateQuestionRequest"
  responses:
    "201":
      description: Created
      content:
        application/json:
          schema:
            $ref: "../../components/schemas/matching_question.yml#/CreateQuestionResponse"
