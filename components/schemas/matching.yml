GetMatchingResponse:
  type: array
  items:
    type: object
    properties:
      matching_id:
        type: string
        format: uuid
      avator_url:
        type: string
      match_user_name:
        type: string
      last_message:
        type: string
      unread_message_count:
        type: integer
