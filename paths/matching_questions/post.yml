post:
  summary: Create a new matching
  tags:
    - MatchingQuestions
  requestBody:
    required: true
    content:
      application/json:
        schema:
          $ref: "../../components/schemas/matching_question.yml#/CreateQuestionReq"
  responses:
    "201":
      description: Created
      content:
        application/json:
          schema:
            $ref: "../../components/schemas/matching_question.yml#/CreateQuestionRes"
