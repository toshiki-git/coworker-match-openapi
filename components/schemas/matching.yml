GetMatchingResponse:
  type: array
  items:
    type: object
    properties:
      matching_id:
        type: string
        format: uuid
      avatar_url:
        type: string
      match_user_name:
        type: string
      last_message:
        type: string
      unread_message_count:
        type: integer

GetMatchingUserResponse:
  type: object
  properties:
    user:
      $ref: "./user.yml#/User"
